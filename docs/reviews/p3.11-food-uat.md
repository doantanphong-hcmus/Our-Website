# P3.11 — Food tests / UAT

Automated status: **passed**.

| Gate | Evidence |
|---|---|
| Vote secrecy | Integration test only returns the current user's votes; no count or partner progress |
| Simultaneous match | Concurrent wants produce exactly one stored match and the same public result for both users |
| All rejected | Unit invariant returns no proxy; E2E renders the safe exhausted state |
| Exclusion/style invariants | Property test runs every style/category/allergen/exclusion combination against the production filter |
| Both styles, two devices | E2E completes Ăn vặt at 390×844 and Ăn no at 360×800 |
| Offline/retry | Food-result retry survives reload with the same idempotency key and respects user isolation |

Run with `npm test`.

Manual content approval remains pending in `content/food.v1.json`; the owner must review the 87 dishes before the Phase 3 milestone is signed off.
